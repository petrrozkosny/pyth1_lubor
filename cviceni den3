'''
Napoveda k bodum  1  - 2.2. zde: https://docs.python-requests.org/en/latest/
1. Importujte modul requests
2. Vytvorte funkci zkontroluj_stranku s jednim pozicnim argumentem stranka
2.1. do promenne r si ulozte navratovou hodnotu metody requests.get()
2.2. pokud se atribut .status_code promenne r nerovna 200, vypiste uzivateli:
    "Stranka {stranka} nefunguje
    jinak vypiste uzivateli:
    "Stranka {stranka funguje}
Bonus
2.3. Osetrete chybu ConnectionError, kdy:
    pokud k chybe nedojde (try je uspesne), vracime body 2.2.zde testujeme, jestli requests.get() bylo uspesne
    pokud dojde k chybe ConnectionError, vypiste uzivateli:
    "Ke strance {stranka} se nebylo mozne pripojit"
Konec bonusu

3. Iterujte listem stranky, kdy kazdou jednu polozku dosadite jako parametr funkce zkontroluj_stranku()

Bonus2
Vytvorte list nefunkci_stranky, ktery bude obsahovat polozky listu stranky, ktere nevratily 
http kod 200
'''
stranky =["https://www.skoleni-ict.cz/kurz/Uvod-do-umele-inteligence-a-strojoveho-uceni--AI1.aspx",
    "https://www.skoleni-ict.cz/blog/multitasking-fungsuje/",
    "https://www.skoleni-ict.cz/kurz/Jazyk-SQL-zaklady-dotazovani-a-manipulace-s-daty-v-SQL-Serveru-SQL1.aspx"
       ]

Bonus3
Pokud je stranka detailem kurzu, tj. URL obsahuje /kurz/, tak přes print vypsat cenu tohoto kurzu

_____________________________________________________________________________________________________


mesta = [['brno','tisnov','kurim'],['olomouc','litovel','prerov',None]]

# Vytvorte si kopii objektu mesta, tu pojmenujte mesta_kopie
# prochazejte objekt mesta_kopie, pokud polozka objektu (tj. list v listu) obsahuje
# None, tak cely objekt (list listu) v mesta_kopie smazte
# Porovnejte delku listu mesta a mesta_kopie, cimz zjistime pocet odebranych polozek
# Pocet odebranych polozek vypiste pres print()

# Vytvorte promennou pocet, nastavte jeji hodnotu na None
# vytvorte promennou slozka, do te ulozte cestu slozky meteo1
# napiste funkci prochazej slozku, ktera nema zadne parametry
# Funkci prochazejte soubory ve slozce, pokud koncovka alespon jednoho souboru
# obsahuje .xlsx, zmente hodnotu promenne pocet na 1
# Na konci behu skriptu vypiste:
# Slozka obsahuje Excel, pokud hodnota promenne pocet je 1
# Slozka neobsahuje Excel, pokud hodnota promenne pocet je None
